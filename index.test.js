
import { describe, it, test, beforeEach, afterEach } from 'node:test'
import assert from 'node:assert'


describe('test', () => {
    beforeEach(async () => {
    })
    afterEach(async () => {
        
    })
    it('should be a cool test', async () => {
     
        assert(1 === 1)
    })

    // Add more tests as needed
})
